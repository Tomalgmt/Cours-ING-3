# Lignes a integrer autour de pam_unix.so dans /etc/pam.d/common-auth.
auth required pam_faillock.so preauth
auth [success=2 default=ignore] pam_unix.so nullok
auth [default=die] pam_faillock.so authfail
